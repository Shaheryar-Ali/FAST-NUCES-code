#include <iostream>
#include <string>
#include "Person.h"

using namespace std;

Person::Person(char nam[], char a[], char nic[], int cel)
{

	name = nam;
	address = a;
	nic_no = nic;
	cel_no = cel;
}